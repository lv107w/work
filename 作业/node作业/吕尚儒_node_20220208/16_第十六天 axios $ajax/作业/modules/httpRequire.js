const url = require('url')
const fs = require('fs')
const path = require('path')
const querystring = require('querystring')
const http = require('http')
const multiparty = require('multiparty')

module.exports = {
  url, fs, path, querystring, http, multiparty
}